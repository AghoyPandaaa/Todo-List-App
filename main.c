#include <stdio.h>
#include <stdlib.h>
#include "tdFuncs.h"


int main() {
    head = addNewTask();
    head = addNewTask();
    head = addNewTask();
    fixTasksNum(head);
    displayTaskTitles(head);
    fixTasksNum(head);
    editTask();



}
